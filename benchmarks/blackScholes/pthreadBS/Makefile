include gppDefs

# required files
GLOBAL_REQUIRE =
BENCH_REQUIRE = options.h
LOCAL_REQUIRE = 

PLFLAGS := $(PLFLAGS) -pthread
PCFLAGS := $(PCFLAGS) -pthread

include ../common/timeRequiredFiles
include MakeBench
