include parallelDefs

# required files
GLOBAL_REQUIRE =
BENCH_REQUIRE = options.h
LOCAL_REQUIRE = 

PCC = icpc
PCFLAGS = -O3 -ltbb $(INTT)
PLFLAGS = -ltbb

include ../common/timeRequiredFiles
include MakeBench
