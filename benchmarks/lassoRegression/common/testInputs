#!/usr/bin/python

bnchmrk="lasso"
benchmark="Lasso"
checkProgram="../common/lassoCheck"
dataDir = "../lassoData/"

tests = [
    [1, "mug_cs.txt", " -l 0.5 -a 1685.0", " -m 1686.0 -l 0.5"],
    [1, "mug_cs.txt", " -l 10.0 -a 30400.0", " -m 30400.0 -l 10.0"],
    [1, "lasso_synthetic_1000_1000_1.txt", " -l 0.5 -a 632", " -m 632 -l 0.5"] 
    ]

testsLong = [
     [1, "finance1000.txt", " -l 0.5 -a 2885", " -m 2885 -l 0.5"]
    ]

import runTests
runTests.timeAllArgs(bnchmrk, benchmark, checkProgram, dataDir, tests)

