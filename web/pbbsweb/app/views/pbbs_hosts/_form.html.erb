<%= form_for(@pbbs_host) do |f| %>
  <% if @pbbs_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pbbs_host.errors.count, "error") %> prohibited this pbbs_host from being saved:</h2>

      <ul>
      <% @pbbs_host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
