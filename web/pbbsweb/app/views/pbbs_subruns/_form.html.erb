<%= form_for(@pbbs_subrun) do |f| %>
  <% if @pbbs_subrun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pbbs_subrun.errors.count, "error") %> prohibited this pbbs_subrun from being saved:</h2>

      <ul>
      <% @pbbs_subrun.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
